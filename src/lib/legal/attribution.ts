/**
 * 밖의 서비스가 요구하는 출처 표기. (설계 문서 15.1절)
 *
 * **이것은 코드가 아니라 약속이다.** TMDB API를 쓰는 조건으로 표기하기로
 * 한 것이며, 지키지 않으면 쓸 자격이 없어진다.
 *
 * 그래서 사용법·개인정보 처리방침과 같은 방식으로 다룬다. 글을 저장소 안에
 * 두고 **검사가 붙잡는다.** (`tests/attribution-coverage.test.mjs`)
 *
 * 말로만 적은 약속은 잊힌다. 화면을 고치다가 문구를 다듬고 싶어지거나,
 * 로고가 눈에 거슬려 지우고 싶어지는 순간이 온다. 그때 검사가 막는다.
 *
 * **고지 문구는 번역하거나 고치지 않는다.** 15.1절이 "다음 공식 고지
 * 문구를 번역하거나 수정하지 않고 그대로 표시한다"고 못 박았다. 이
 * 저장소의 글이 전부 한글이라 이 한 줄만 영문으로 남는 것이 어색해
 * 보이는데, **어색함이 지켜야 할 모양이다.**
 *
 * 이 파일에 다른 것을 import하지 않는다. 검사가 이 값만 따로 들여다볼 수
 * 있어야 한다.
 */

export type Attribution = {
  /** 어느 서비스인가. */
  id: string;
  /** 화면에 보이는 이름. */
  name: string;
  /** 그 서비스가 우리에게 해주는 일. 왜 이 표기가 있는지 설명한다. */
  role: string;
  /** 공식 사이트. 15.1절 2번이 요구하는 링크다. */
  siteUrl: string;
  /**
   * 공식 로고 파일. `public/` 아래의 주소다.
   *
   * **공식 승인 로고 파일만 쓴다.** 색·비율·방향을 바꾸지 않는다.
   * (15.1절 표시 원칙)
   *
   * **없을 수 있다.** 로고까지 요구하는 곳이 있고 글로 밝히면 되는 곳이
   * 있다. TMDB는 15.1절이 로고를 명시적으로 요구하지만, JustWatch는
   * "출처를 JustWatch로 밝히라"는 것이 요구다.
   *
   * 없는데 있는 척 그리지 않는다. 아무 그림이나 가져다 쓰면 **공식 파일만
   * 쓴다는 조건을 어기는 것**이고, 빈 자리를 그리면 표기가 깨져 보인다.
   * 나중에 로고가 필요해지면 파일을 받아 여기에 주소를 적는다.
   */
  logoSrc?: string;
  logoAlt?: string;
  /**
   * 고치지 않고 그대로 보여야 하는 문구.
   *
   * **번역하지 않는다.** 검사가 글자 그대로 견준다.
   */
  notice: string;
  /** 표기 조건이 적힌 곳. 나중에 조건이 바뀌었는지 볼 수 있어야 한다. */
  termsUrl: string;
};

export const ATTRIBUTIONS: readonly Attribution[] = [
  {
    id: "tmdb",
    name: "TMDB",
    role: "영화와 드라마의 정보와 포스터를 가져옵니다.",
    siteUrl: "https://www.themoviedb.org",
    logoSrc: "/tmdb.svg",
    logoAlt: "TMDB 로고",
    /*
      15.1절 3번의 문구다. **한 글자도 바꾸지 않는다.**
      검사가 이 값과 화면에 그려지는 값을 함께 본다.
    */
    notice:
      "This product uses the TMDB API but is not endorsed or certified by TMDB.",
    termsUrl: "https://www.themoviedb.org/about/logos-attribution",
  },
  {
    id: "justwatch",
    name: "JustWatch",
    role: "영화와 드라마를 어디서 볼 수 있는지 알려줍니다.",
    siteUrl: "https://www.justwatch.com",
    /*
      로고 파일을 두지 않는다.

      TMDB는 15.1절이 공식 로고를 명시적으로 요구하지만, JustWatch 쪽
      요구는 **출처를 JustWatch로 밝히는 것**이다. 아래 문구와 링크가
      그 일을 한다.

      **로고까지 요구하는지 우리가 확실히 확인하지 못했다.** 그래서 아무
      그림이나 가져다 쓰지 않는다. 공식 파일만 쓴다는 조건이 있는 자리에
      짐작으로 그림을 넣는 것이 가장 나쁘다. 필요하다고 확인되면 그때
      파일을 받아 `logoSrc`를 적는다.
    */
    notice:
      "볼 수 있는 곳 정보는 JustWatch에서 제공하며, TMDB를 통해 전달받습니다.",
    termsUrl: "https://developer.themoviedb.org/docs/faq",
  },
  {
    id: "kakao",
    name: "Kakao",
    role: "책 정보와 장소의 주소·좌표를 가져옵니다.",
    siteUrl: "https://developers.kakao.com",
    /*
      로고를 두지 않는다. 문구도 우리가 쓴 것이다.

      **카카오가 정해진 고지 문구를 요구하는지 확인하지 못했다.** 찾아본
      것은 개발자 문서와 데브톡의 문의 글들인데, 지도를 **그려서** 보여줄
      때의 저작권 표시를 묻는 이야기가 대부분이고 **로컬 API로 받아온
      주소를 글로 보여줄 때** 어떤 문구를 써야 하는지는 나오지 않았다.

      그래서 TMDB처럼 글자 그대로 지켜야 하는 문구가 아니다. JustWatch와
      같은 자리다. **어디서 온 값인지 밝히는 것**이 우리가 지킬 수 있는
      것이고, 아래 문구와 링크가 그 일을 한다.

      **짐작으로 영문 고지를 지어내지 않는다.** 공식 문구가 아닌 것을
      공식처럼 적으면, 나중에 진짜 요구를 만났을 때 이미 지키고 있다고
      착각하게 된다. 지어낸 문구는 안 적은 것보다 나쁘다.

      로고도 같은 이유로 두지 않는다. 공식 파일만 쓴다는 조건이 있는
      자리에 아무 그림을 넣는 것이 가장 나쁘다. (JustWatch와 같은 판단)

      **2026-09-25에 장소(17-1)를 만들면서 더했다.** 그전까지 책 검색만
      Kakao를 썼는데 이 목록에 없었다. 방침 표(`legal/content.ts`)에는
      있었지만 표기 화면에는 없었다. 두 곳이 갈라져 있던 것이다.
    */
    notice:
      "책 정보와 장소의 주소·좌표는 Kakao Developers의 API에서 가져옵니다.",
    /*
      요구 조건이 적힌 페이지를 찾지 못해 API 문서를 가리킨다. **이 링크가
      조건을 보증하지 않는다.** 조건이 적힌 곳을 찾으면 그때 바꾼다.
    */
    termsUrl: "https://developers.kakao.com/docs/ko/local/common",
  },
];

/**
 * 로고를 우리 이름보다 덜 두드러지게 놓는 크기.
 *
 * 15.1절: "TMDB 로고는 ThreadMark의 이름이나 로고보다 덜 두드러지게
 * 표시한다." 화면 여러 곳에서 이 값을 쓰게 될 때 제각각이 되지 않도록
 * 여기 둔다.
 */
export const ATTRIBUTION_LOGO_HEIGHT = 20;
