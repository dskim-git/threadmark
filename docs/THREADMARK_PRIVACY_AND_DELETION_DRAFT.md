# ThreadMark 개인정보 처리방침 및 데이터 삭제 안내 — 초안

> 작성일: 2026-09-19  
> 상태: 개발·내부 테스트용 초안  
> **공개본은 `/privacy`와 `/data-deletion`이며 글의 원본은 `src/lib/legal/content.ts`다.**
> 이 문서는 그 글의 근거와 아직 정하지 못한 것을 적어두는 곳으로 남긴다.
> 두 곳에 같은 글을 두지 않는다. (2026-09-24, 17-B)  
> 운영자 표시명: ThreadMark Admin  
> 문의: daesobi1@gmail.com  
> 서비스 주소: https://thread-mark.vercel.app

이 문서는 ThreadMark의 개발 및 제한적 테스트 단계에서 사용할 개인정보 처리방침과 데이터 삭제 절차 초안이다. 공개 서비스로 전환하기 전에 실제 구현, 이용 대상, 외부 서비스의 최신 약관, 개인정보의 국외 이전 세부사항 및 대한민국 관련 법령을 다시 검토해야 한다. 이 초안은 법률 자문을 대신하지 않는다.

## 1. 서비스 운영 방식

ThreadMark는 연구, 수업, 독서 및 일상에서 발견한 자료와 생각을 Source, Capture, Project 단위로 저장하고 연결하는 개인 지식 관리 서비스다.

초기에는 회원가입 후 관리자의 승인을 받은 사용자만 이용할 수 있다. `신규 가입 승인 필요` 설정이 켜진 동안 신규 계정은 승인 대기 상태가 되며, 관리자가 승인하기 전에는 개인 자료 저장 기능에 접근할 수 없다. 향후 이 설정을 끄면 그 이후의 신규 가입자는 자동 승인될 수 있다. 설정을 끄더라도 이미 승인 대기 중인 계정은 자동으로 승인되지 않는다.

## 2. 처리하는 개인정보

ThreadMark는 서비스 제공에 필요한 범위에서 다음 정보를 처리할 수 있다.

| 구분 | 항목 | 목적 |
| --- | --- | --- |
| 계정·인증 | 이메일 주소, 인증 제공자, Supabase 사용자 ID, 가입·최근 로그인 시각 | 회원 식별, 로그인, 계정 보안 |
| 프로필 | 표시 이름, 프로필 이미지 URL, 선호 언어, 시간대 | 개인화 및 화면 표시 |
| 가입 승인 | 승인 상태, 신청·승인·거절·정지 시각, 처리 관리자, 운영상 필요한 사유 | 제한적 운영, 접근 통제, 보안 감사 |
| 사용자 콘텐츠 | Source, Capture, Project, 태그, 메모, 인용, 번역, 요약, 관계 정보 | 핵심 지식 관리 기능 제공 |
| 파일 메타데이터 | Google Drive 파일 ID, 파일명, MIME type, 크기, 폴더 ID, 위치 정보 | 사용자가 선택한 파일 연결 및 다시 열기 |
| Google 연결 | OAuth access token, 암호화된 refresh token, 권한 범위, 연결·갱신 시각 | 사용자가 허용한 Google Drive 파일 접근 |
| AI 요청 | 사용자가 직접 선택하고 실행한 텍스트, 요청 유형, 결과, 모델 및 생성 시각 | 번역, 요약, 키워드·태그 및 아이디어 생성 |
| 기술·보안 정보 | 접속 시각, 오류 기록, 요청 식별자, 기기·브라우저의 제한된 정보, 보안 이벤트 | 장애 대응, 부정 이용 방지, 보안 유지 |

ThreadMark는 주민등록번호, 금융정보, 건강정보 등 핵심 기능에 필요하지 않은 민감한 개인정보를 의도적으로 요구하지 않는다. 사용자는 메모나 파일에 타인의 개인정보 또는 민감정보를 불필요하게 입력하지 않아야 한다.

## 3. Google Drive 데이터 사용

ThreadMark는 Google Drive 전체 접근 권한 대신 `https://www.googleapis.com/auth/drive.file` 범위를 사용한다. 이에 따라 사용자가 ThreadMark를 통해 만들거나 Google Picker로 명시적으로 선택한 파일을 중심으로 접근한다.

- 사용자의 Drive 파일 자체는 원칙적으로 사용자의 Google Drive에 저장된다.
- ThreadMark 데이터베이스에는 파일을 다시 찾기 위한 ID와 최소한의 메타데이터를 저장할 수 있다.
- 비공개 Drive 파일을 자동으로 공개 링크로 변경하지 않는다.
- Google refresh token은 서버 전용 영역에 암호화하여 저장한다.
- Google 사용자 데이터는 사용자에게 보이는 파일 연결·열기·저장 기능을 제공하는 목적으로만 사용한다.
- Google 사용자 데이터를 판매하거나 광고, 신용평가 또는 데이터 중개 목적으로 사용하지 않는다.
- 운영자는 사용자의 명시적 동의, 보안 사고 조사 또는 법적 의무가 있는 경우를 제외하고 사용자의 Drive 파일 내용을 임의로 열람하지 않는다.

**Google Limited Use 고지:** ThreadMark의 Google API에서 수신한 정보의 사용 및 다른 앱으로의 전송은 Limited Use 요건을 포함한 [Google API Services User Data Policy](https://developers.google.com/terms/api-services-user-data-policy)를 준수한다.

## 4. 생성형 AI 사용

ThreadMark는 Anthropic Claude API를 부분 번역, 짧은 요약, 키워드·태그 후보 및 연구·수업 아이디어 생성에 사용할 수 있다.

- 사용자가 AI 기능을 명시적으로 실행한 경우에만 선택된 텍스트를 전송한다.
- PDF 전체 또는 Google Drive 파일 전체를 자동으로 전송하지 않는다.
- AI 요청 직전에 전송 범위와 목적을 사용자가 확인할 수 있게 한다.
- AI 생성 결과는 원문 및 사용자 작성 내용과 구분한다.
- 광고 또는 범용 AI 모델 학습을 목적으로 Google 사용자 데이터를 제공하지 않는다.
- 실제 공개 전 Anthropic의 당시 데이터 보존·학습 설정과 계약 조건을 다시 확인하고 방침에 반영한다.

## 5. 보유 및 이용 기간

개인정보는 원칙적으로 회원 탈퇴 또는 처리 목적 달성 시까지 보유한 뒤 삭제한다.

| 정보 | 보유 기준 |
| --- | --- |
| 계정·프로필·가입 승인 정보 | 회원 탈퇴 또는 계정 삭제 완료 시까지 |
| Source·Capture·Project와 연결 메타데이터 | 사용자가 삭제하거나 회원 탈퇴할 때까지 |
| Google OAuth token | Drive 연결 해제 또는 회원 탈퇴 시까지 |
| AI 요청·결과 | 사용자가 해당 결과를 삭제하거나 회원 탈퇴할 때까지. 별도 서버 로그에는 원문을 남기지 않는 것을 원칙으로 함 |
| 보안·오류 로그 | 보안과 장애 대응에 필요한 최소 기간. 공개 전 구체적 기간 확정 필요 |
| 백업 데이터 | 활성 데이터에서 삭제 후 정기 백업 순환에 따라 삭제. 공개 전 최대 잔존 기간 확정 필요 |

관련 법령에 별도 보존 의무가 생기는 경우 해당 정보만 분리하여 법정 기간 동안 보관할 수 있으며, 그 근거와 기간을 이 방침에 추가한다.

## 6. 개인정보 제공 및 처리위탁·국외 이전

ThreadMark는 사용자의 개인정보를 판매하지 않는다. 서비스 운영을 위해 다음 외부 서비스가 개인정보 처리에 관여할 수 있다.

| 서비스 | 역할 | 처리 가능 정보 |
| --- | --- | --- |
| Supabase | 인증, 데이터베이스, 서버 기능 | 계정, 프로필, 사용자 콘텐츠와 메타데이터 |
| Vercel | 웹 애플리케이션 호스팅과 서버 실행 | 요청 정보, 서버 로그, API 요청 처리 데이터 |
| Google | 로그인, Drive, Picker, YouTube API | Google 계정 식별 정보, 사용자가 선택한 Drive 파일 정보 |
| Anthropic | 사용자가 실행한 AI 처리 | 사용자가 확인하고 전송한 선택 텍스트와 요청 |
| Kakao·TMDB 및 기타 메타데이터 공급자 | 자료 검색 및 메타데이터 조회 | 검색어, ISBN, 외부 콘텐츠 ID 등 기능 수행에 필요한 값 |

일부 제공자는 대한민국 외 지역의 서버 또는 글로벌 인프라를 사용할 수 있다. 공개 전 각 제공자의 최신 하위처리자 목록과 계약을 확인하여 이전 국가, 이전 일시·방법, 이전 항목, 이용 목적, 보유 기간 및 거부 방법을 구체적으로 확정해 이 절을 갱신한다.

## 7. 이용자의 권리와 행사 방법

이용자는 자신의 개인정보와 콘텐츠에 대해 열람, 정정, 삭제, 처리정지, 동의 철회 및 계정 삭제를 요청할 수 있다.

- 앱의 설정 화면에서 프로필 및 콘텐츠를 수정·삭제할 수 있도록 구현한다.
- 계정 및 전체 데이터 삭제 기능은 `/account/delete`에서 제공한다. (2026-09-24 구현, 17-A)
  설정 화면과 승인 상태 안내 화면(`/pending`) 양쪽에서 이 화면으로 갈 수 있다.
- 로그인할 수 없거나 계정의 이메일 주소를 확인할 수 없는 경우 `daesobi1@gmail.com`으로 요청할 수 있다.
- 운영자는 본인 확인 후 요청을 처리하고 결과를 안내한다.
- 승인 대기 또는 거절 상태의 사용자도 계정 삭제를 요청할 수 있다.

## 8. Google Drive 연결 해제

Google Drive 연결 해제는 계정 삭제와 별도로 할 수 있다.

1. ThreadMark의 설정에서 `Google Drive 연결 해제`를 선택한다.
2. ThreadMark는 저장된 Google OAuth token을 폐기하고 연결 상태를 해제한다.
3. 사용자는 [Google 계정의 서드 파티 연결 관리](https://myaccount.google.com/connections)에서도 ThreadMark의 접근 권한을 철회할 수 있다.
4. 연결 해제 후에도 사용자가 별도로 삭제하지 않은 Source와 Capture 메타데이터는 유지될 수 있지만, Drive 파일을 다시 열려면 재연결이 필요하다.
5. Google Drive에 저장된 원본 파일은 사용자의 소유이므로 연결 해제만으로 자동 삭제하지 않는다.

## 9. 계정 및 데이터 삭제 절차

### 앱에서 삭제

경로: `설정 → 계정 지우기` (`/account/delete`). 승인 대기·거절·정지 상태에서는
`/pending` 화면 아래쪽의 같은 링크로 들어간다. 로그인만 되어 있으면 된다.

이 화면은 삭제 전에 다음을 나눠서 표시한다.

- **함께 사라지는 것** — 계정과 프로필, Source, Capture, Project, 태그, 중요 표시,
  논문 정보·분석·활용 계획, 자료 관계, 웹사이트 정보, 음악 정보와 링크,
  파일 메타데이터, 저장된 Google OAuth token
- **남는 것** — 사용자의 Google Drive에 있는 원본 파일, 그리고 관리자 감사 기록
  (감사 기록에는 상태 변경 시각과 값만 담기며 이메일이나 이름은 담기지 않는다)
- 되돌릴 수 없다는 사실과, 운영자도 복구할 수 없다는 사실

확인은 **계정의 이메일 주소를 직접 입력**하는 것으로 한다. 화면에 표시된 주소와
대소문자·앞뒤 공백을 무시하고 비교하며, 서버에서 다시 검증한다. 여러 계정을
쓰는 사용자가 어느 계정을 지우는지 눈으로 확인하게 하는 것이 목적이다.

처리 순서는 다음과 같다.

1. 하나뿐인 관리자인지 확인한다. 그런 경우 삭제하지 않는다. (아래 참고)
2. Google OAuth 권한을 폐기하고 저장된 token을 삭제한다.
3. Supabase Auth 계정을 삭제한다. 사용자 소유의 모든 ThreadMark 데이터는
   외래키의 `on delete cascade`로 함께 삭제된다.
4. 브라우저 세션을 정리하고 로그인 화면에서 삭제 완료를 안내한다.
5. 실패한 경우 사용자 콘텐츠는 삭제되지 않았음을 알린다. 다만 2단계가 이미
   수행되었을 수 있으므로 Drive 연결이 해제되었을 가능성을 함께 안내한다.
   "아무것도 삭제되지 않았다"고 단정하지 않는다.

**중간 상태를 두지 않는다.** 초안은 "삭제 처리 중" 상태로 전환한 뒤 처리하도록
적고 있었으나, 그 상태에서 중단된 계정을 복구하는 절차를 별도로 만들어야 하고
현재 데이터 규모에서는 전체 처리가 수 초 내에 끝난다. 데이터가 크게 늘어나면
다시 검토한다.

**Google Drive의 원본 파일은 삭제하지 않는다.** 사용자 소유의 파일이며 복구할
수 없기 때문이다. 화면에서 파일이 남는다는 사실과 Drive에서 직접 삭제하는
방법, Google 계정의 서드 파티 연결 관리 페이지를 함께 안내한다.

**하나뿐인 관리자는 자신의 계정을 삭제할 수 없다.** 삭제되면 신규 가입 승인을
처리할 수 있는 사람이 없어지고, 앱 안에서는 복구할 방법이 없다. 이 경우 화면은
다른 사용자에게 관리자 권한을 먼저 부여하도록 안내한다. 데이터베이스의
`guard_last_admin` 트리거는 service role에 대해 예외를 두므로, 이 확인은
애플리케이션에서 별도로 수행한다.

### 이메일로 삭제 요청

다음 정보를 포함하여 `daesobi1@gmail.com`으로 요청할 수 있다.

- 제목: `ThreadMark 계정 삭제 요청`
- 가입한 이메일 주소
- 요청 범위: 계정 전체 삭제, 특정 데이터 삭제 또는 Google Drive 연결 해제

비밀번호, OAuth token, API key 또는 신분증 전체 사본은 이메일로 보내지 않는다. 추가 본인 확인이 필요한 경우 최소한의 안전한 절차를 별도로 안내한다.

## 10. 개인정보 보호조치

ThreadMark는 다음 보호조치를 적용하거나 구현할 예정이다.

- HTTPS를 통한 전송 암호화
- 사용자 데이터별 `owner_id`와 Supabase RLS 적용
- 승인 상태와 역할을 서버 및 RLS에서 재검증
- Supabase secret key와 외부 API 비밀값의 서버 전용 보관
- Google refresh token의 암호화 저장
- 최소 권한 Google Drive scope 사용
- 기본 비공개 설정과 명시적 공유 절차
- 관리자 승인·거절·정지 및 설정 변경 감사 로그
- 로그에서 token, API key, 원문 및 민감정보 제거
- 정기적인 의존성·권한·보안 설정 점검

## 11. 만 14세 미만 이용자

초기 ThreadMark는 만 14세 미만 아동을 대상으로 하지 않는다. 만 14세 미만 사용자를 대상으로 제공하려면 법정대리인 동의와 별도 보호 절차를 구현하고 이 방침을 개정한 뒤에만 허용한다.

## 12. 쿠키와 세션

ThreadMark는 로그인 상태 유지와 보안을 위해 필수 인증 쿠키 또는 유사한 세션 기술을 사용할 수 있다. 광고 추적이나 맞춤형 광고를 위한 쿠키는 사용하지 않는다. 분석 도구를 추가할 경우 수집 항목, 목적, 보유 기간 및 거부 방법을 사전에 고지한다.

## 13. 방침 변경

이 방침을 변경할 경우 시행일과 변경 내용을 서비스 안에서 알린다. 개인정보 또는 Google 사용자 데이터의 이용 목적이 실질적으로 달라지는 경우, 변경된 처리를 시작하기 전에 필요한 고지와 동의 절차를 진행한다.

## 14. 개인정보 관련 문의

- 운영자: ThreadMark Admin
- 이메일: daesobi1@gmail.com
- 서비스: https://thread-mark.vercel.app

## 15. 공개 전 필수 확인 목록

- [완료] `/privacy`와 `/data-deletion` 공개 (17-B)
- [완료] 로그인 화면에서 개인정보 처리방침과 삭제 안내 링크 제공 (17-B)
- Google Drive 권한 요청 직전에 접근 데이터와 사용 목적을 별도 화면으로 고지하고 명시적 동의 획득
- Google OAuth 동의 화면에 홈페이지, 개인정보 처리방침 및 삭제 안내 URL 등록
  (등록할 주소가 준비되었다: `https://thread-mark.vercel.app/privacy`,
  `https://thread-mark.vercel.app/data-deletion`)
- Google 앱을 `테스트 중`에서 `프로덕션`으로 올리기. 지금은 테스터로 등록한
  사람만 로그인할 수 있고, Drive 재연결 권한이 7일 만에 만료된다고 알려져 있다
- Supabase, Vercel, Google, Anthropic 등 국외 이전·위탁 세부사항(이전 국가·시점·
  방법·거부 방법) 확정. **어느 곳을 쓰는지 자체는 17-B에서 바로잡았다.**
  공개본의 목록이 `src/lib/legal/content.ts`의 `EXTERNAL_PROCESSORS`이고,
  `tests/legal-coverage.test.mjs`가 밖으로 요청을 보내는 곳과 맞춰 본다.
  아래 6절 표는 초안 당시의 기록이며 공개본과 다르다.
- **로그 및 백업의 실제 보유 기간 확정.** Vercel의 런타임 로그 보관 기간과
  Supabase 요금제의 백업 정책을 대시보드에서 확인해야 한다. 공개본 5절은
  지금 "저희가 정하는 값이 아니라 확인해서 적겠다"고 밝혀 두었다.
- [구현 완료, 실제 확인 필요] 계정 삭제, Drive 연결 해제, token 폐기 및 부분 실패 복구 테스트
- 관리자 승인 모드와 자동 승인 모드 모두에 대한 권한·RLS 테스트
- 공개 대상에 미성년자가 포함될 경우 연령과 동의 절차 추가 검토
- 공개 서비스 전환 전 개인정보보호 전문가 또는 법률 전문가 검토

## 참고한 공식 자료

- [개인정보 처리방침 작성지침 — 개인정보보호포털](https://www.privacy.go.kr/)
- [개인정보 보호법 — 국가법령정보센터](https://www.law.go.kr/법령/개인정보보호법)
- [Google API Services User Data Policy](https://developers.google.com/terms/api-services-user-data-policy)
- [Google Workspace User Data and Developer Policy](https://developers.google.com/workspace/workspace-api-user-data-developer-policy)

